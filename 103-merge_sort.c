#include <stdio.h>
#include <stdlib.h>

void merge_sort(int *array, size_t size)
{
    size_t mid = 0, i, j;
    int *left, *right;
    
    mid = (size/2);

    for (i = 0; i < mid; i++)
    {
        
    }

}